# SSTI

<img width="651" height="382" alt="image" src="https://github.com/user-attachments/assets/a6583427-c83b-499f-8cf3-26637d1d2ea3" />

# Поиск файлов по имени и по содержимому

Поиск файла по названию:

```
find / -name 'flag*'
```

Поиск файла по содержимому:

```
grep -rE 'flag[0-9]+\{[^}]+\}' / # Ищем флаги формата flagN{random_string}
```

Или:

```
grep -r "flag*" /
```

Если файл бинарный:

```
grep -r --binary-files=text "flag*" /
```

# Брут приватного ключа SSH

```
ssh2john ssh_key > hash_key
```

```
john --wordlist=wordlist hash_key
```
